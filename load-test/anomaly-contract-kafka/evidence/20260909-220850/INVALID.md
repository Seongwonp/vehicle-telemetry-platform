# 이 실행은 완주하지 않았다 — 내가 중단시켰다

`status.txt`가 `RUNNING`인 채로 남아 있다. 완주하면 `evidence_finish`가 `COMPLETE`로
바꾸므로, **RUNNING은 중단됐다는 표시**다(2026-09-07에 만든 규칙).

## 왜 중단했나

시나리오 2에서 DLQ 발행 실패를 **도달 불가 브로커**(`10.255.255.1:9092`)로 주입하려 했는데,
kafka-python의 `KafkaProducer` **생성자가 부트스트랩에서 블록**했다 —
`max_block_ms`는 `send()`에만 걸린다. 5분을 멈춰 있길래 죽였다.

**고친 것**: 진짜 브로커에 붙은 진짜 producer에 `max_request_size=1`을 줘서
`send()`가 즉시 실패하게 했다. 더 약한 주입이고, 그 대가는
`RESULT_20260909_kafka_contract.md`의 "무엇을 검증하지 않았나" 절에 적었다.

## 지우지 않는 이유

`CLAUDE.md` 실험 규칙 7번 — "실패한 실행과 잘못된 측정 도구도 지우지 않고 무효 사유를 적는다."

유효한 실행은 `../20260909-223547`(4/4 PASS)이다.
