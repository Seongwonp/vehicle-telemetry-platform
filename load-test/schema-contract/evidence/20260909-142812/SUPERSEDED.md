# 이 실행은 유효하지만 **대체됐다** — 최종 코드가 아니다

19/19 PASS는 그 시점 코드에 대해 유효하다. 무효가 아니다.

다만 이 실행 **뒤에** `TelemetryDecoder`에 JSON `null` 리터럴 가드가 추가됐다
(이미지 빌드 14:21 / 가드 14:39). **검증 대상과 최종 코드가 다르면 그 검증은
최종 코드의 근거가 아니다.**

시나리오도 `null_literal` 두 칸이 늘었다(9종 → 10종).

**최종 코드에 대한 E2E 근거는 `../20260909-145736`(21/21)이다.**

사후 분석은 `derived/`에 있고 그 안의 `PROVENANCE.md`가 생성 방법과 재현 조건을 적는다.
