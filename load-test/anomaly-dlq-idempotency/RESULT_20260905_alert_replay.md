# 이상 알림 DLQ 재처리 멱등성 — 실측 (2026-09-05)

| 항목 | 값 |
| --- | --- |
| **검증 상태** | **부분 검증** — 각 조건 1회 실행 |
| 적용 범위 | 단일 Docker Compose, 50대 / 0.2초 / 이상률 0.3, PostgreSQL 장애 60초·300초 |
| 코드 상태 | 배치화 전: `658eb7f` / 배치화 후: `2b7459e`·`f3f97e4` / 트랜잭션 버그 수정 후: `856f66a` |
| 실행 명령 | `bash load-test/anomaly-dlq-idempotency/run_scenario.sh <초>` |
| 환경 | [`docs/verification/2026-09-05-environment.md`](../../docs/verification/2026-09-05-environment.md) |
| 원본 증거 | **보존하지 않음** — 아래 "이 문서의 한계" 참고 |
| 회귀 고정 | `AnomalyServiceTest`(멱등·전파), `TelemetryConsumerTest`(중복 브로드캐스트 차단) — **회귀 검증** |

> **1회 실행이다.** 아래 수치는 "이 조건에서 관찰된 값"이다.
> 특히 in-doubt 건수(9건 중 3건 등)는 장애 주입 타이밍에 크게 좌우되므로
> 반복하면 달라질 값이다.

CLAUDE.md 우선순위 2번의 남은 갈래. DLQ 재처리 도구와 Runbook을 만들면서
`vehicle-anomaly-alerts-dlq` 재처리가 PostgreSQL에 알림을 **중복 저장하는지**는
재보지 않았다. 텔레메트리 쪽은 InfluxDB가 같은 키를 덮어써서 멱등이 확인됐지만,
알림은 `INSERT`라 같은 보장이 없다. 운영자에게 "이 명령으로 재처리하세요"라고
문서에 써둔 경로라서, 여기서 중복이 생기면 문서가 사고를 유도하는 셈이었다.

**결론: 행 중복은 0건이다. 대신 중복 *알림*이 나가고 있었고, 그건 고쳤다.**

절차: `bash load-test/anomaly-dlq-idempotency/run_scenario.sh 60`

---

## 무엇을 "정답"으로 삼았나

토픽의 메시지 수가 아니라 **고유 `event_id` 수**다.

`anomaly_alerts.event_id`에는 UNIQUE 인덱스가 있고 저장은
`INSERT … ON CONFLICT (event_id) DO NOTHING`이다. 즉 설계상 "같은 event_id는 한 행"이라,
대조해야 할 것은 **행 수 == 토픽의 고유 event_id 수**다.

`event_id`는 `vehicle_id|timestamp|anomaly_type|field|detector`의 SHA-256이고
Python(발행)과 Java(저장)가 같은 식을 쓴다. 실측에서 토픽 16,636건 중

- `event_id` 없음: **0건**
- 발행값이 파생값과 불일치: **0건**
- 내용이 다른데 event_id가 같음(뭉개짐): **0건**

이라 이 기준을 그대로 쓸 수 있었다. 뭉개짐 0건은 중요하다 — event_id가 서로 다른
이상을 한 행으로 합쳐버리면 그건 중복의 반대편, **알림 유실**이기 때문이다.
(같은 차량이 같은 밀리초에 같은 종류의 이상을 두 번 내면 이론상 가능하다.
시뮬레이터 타임스탬프가 밀리초 정밀도라 이 부하에서는 일어나지 않았다.)

## 시나리오

50대 / 0.2초 / 이상률 0.3으로 60초 정상 구간 → **PostgreSQL 60초 정지** → 복구 →
부하 정지 후 드레인 → 재처리 2회.

2회차는 **일부러 다른 커서 그룹**으로 돌려 같은 레코드를 다시 읽게 했다.
운영자가 "아까 재처리가 먹었나?" 하고 한 번 더 실행하는 상황이고,
여기서 안전하지 않으면 Runbook이 틀린 것이다.

---

## 발견 1 — PostgreSQL 장애는 DLQ를 거의 만들지 않는다. 컨슈머가 멈춘다

60초 장애에서 DLQ로 간 알림은 **9건뿐**이었다. 그 사이 토픽에는 수천 건이 들어왔다.

로그의 시각이 원인을 그대로 보여준다.

| 시각 | 건수 | 예외 |
| --- | ---: | --- |
| 장애 발생 즉시 | 3 | `DataAccessResourceFailureException: Unable to commit … terminating connection` |
| +30초 | 3 | `CannotCreateTransactionException: Could not open JPA EntityManager` |
| +60초 | 3 | 〃 |

30초 간격은 HikariCP `connectionTimeout` 기본값이다. 파티션 3개 × 30초에 1건 =
**초당 0.1건**. 실패로 빠르게 떨어져 DLQ로 가는 게 아니라, 컨슈머가 **30초씩 붙잡혀 있다.**
나머지는 전부 lag으로 쌓였고 복구 후 천천히 따라잡았다.

이번 60초 장애에서는 리밸런싱이 없었다(`generation 1` 유지, `poll timeout` 로그 0건).
다만 `max.poll.interval.ms`가 300초이고 이 속도라면 **한 poll 주기에 10건**밖에
처리하지 못하므로, 장애가 5분을 넘기면 리밸런싱이 돌 수 있다. **그건 재지 않았다.**

## 발견 2 — 재처리는 "혹시"가 아니라 **반드시** 중복 INSERT를 시도한다

DLQ 9건의 `event_id`를 테이블과 대조했더니 **3건은 이미 저장돼 있었다.**

그 3건이 정확히 `Unable to commit` 유형이다. 서버에서는 커밋이 끝났는데 연결이
끊겨 **클라이언트만 실패로 본** 경우다(in-doubt transaction). 나머지 6건은
연결 자체를 못 얻은 것이라 진짜로 저장되지 않았다.

숫자가 정확히 맞는다: 토픽 16,636 − 진짜 유실 6 = **행 16,630**.

이 3건 덕분에 이 측정이 의미를 갖는다. 재처리 대상 중 이미 저장된 것이 0건이면
중복이 날 기회 자체가 없어서 "중복이 안 났다"가 아무것도 증명하지 못한다.
그래서 스크립트는 이 값을 먼저 찍고, 0이면 그렇다고 경고한다.

## 발견 3 — 행 중복은 0건. 하지만 알림은 매번 다시 나갔다

| | PostgreSQL 행 | `[이상 저장]` 로그 | 토픽 | DLQ |
| --- | ---: | ---: | ---: | ---: |
| 재처리 전 | 16,630 | 16,630 | 16,636 | 9 |
| 1회차 후 | **16,636** (+6) | 16,639 (+9) | 16,645 | 9 |
| 2회차 후 | **16,636** (+0) | 16,648 (+9) | 16,654 | 9 |

- **복구**: 행이 정확히 6건 늘어 토픽의 고유 event_id 수(16,636)와 **일치**했다.
  유실 0. 행 수 == 고유 event_id 수도 계속 성립했다(16,636 = 16,636).
- **중복**: 2회차에 같은 9건을 전부 다시 넣었는데 **행은 하나도 늘지 않았다.**
  `UNIQUE(event_id)` + `ON CONFLICT DO NOTHING`이 실제로 동작한다.
- **그런데 `[이상 저장]` 로그는 매번 9건씩 늘었다.** 1회차에 3건, 2회차에 9건이
  실제로는 아무것도 저장하지 않은 로그다.

로그만의 문제가 아니었다. `TelemetryConsumer.consumeAnomalyAlerts`가
`insertIfAbsent`의 반환값을 버리고 **무조건 WebSocket 브로드캐스트**를 했다.
즉 재처리할 때마다 같은 알림이 `/topic/vehicle/{id}/anomalies`로 다시 나갔다.

> **영향 범위는 정직하게 적는다.** Flutter 앱은 현재 `/telemetry`만 구독하고
> 이상 이력은 REST로 가져온다(`lib/core/providers/anomaly_providers.dart`).
> 그래서 **지금 이 중복을 받는 클라이언트는 없다.** 잠재적 결함이고,
> 당장 눈에 보이는 피해는 "재처리 후 로그를 세면 저장 건수가 부풀려진다"는 것이다.

### 고친 것

`AnomalyService.save()`가 `SaveResult(alert, inserted)`를 돌려주고,
컨슈머는 `inserted`일 때만 브로드캐스트한다. 로그도 `[이상 저장]` / `[이상 중복]`으로
나누고, 지표 `telemetry.anomaly.stored{result=new|duplicate}`를 추가했다.

**고친 뒤 같은 9건을 3회차로 다시 재처리해 확인했다:**

```
행: 16,636 → 16,636 (+0)
[이상 저장] 0건   [이상 중복] 9건
telemetry_anomaly_stored_total{result="duplicate"} 9.0
```

---

---

## 후속 재측정 (같은 날, 배치화 이후)

이 문서의 위 결과를 낸 뒤 알림 저장 경로를 배치화했다(ADR-022). 같은 시나리오를
다시 돌려 **바뀐 동작을 확인하고 멱등성 회귀도 함께 봤다.**

| | 배치화 전 | 배치화 후 |
| --- | ---: | ---: |
| 정상 60초 구간의 저장 행 | 1,502 | **5,739** |
| 장애 중 DLQ | 9 | **6** |
| 드레인 소요 | 5분 이상, lag 3,693 잔존 | **28초, lag 0** |
| 토픽 고유 event_id | 16,636 | 16,863 |
| PostgreSQL 행 | 16,630 | **16,863** |
| **유실** | **6건** | **0건** |

**가장 큰 변화는 유실이 사라진 것이다.** 예전에는 DB 장애 중 저장 실패가 곧바로
DLQ로 갔고, 그중 6건은 진짜로 저장되지 않아 **사람이 재처리해야만 복구됐다.**
이제는 재시도 예산(180초)을 타므로 PostgreSQL이 살아나면 자동으로 저장된다.

DLQ에 남은 6건은 **전부 이미 저장돼 있는 것**이었다(`6 / 6`) — 서버 커밋은 끝났는데
연결이 끊겨 클라이언트만 실패로 본 in-doubt 건이다. 즉 **DLQ에 복구가 필요한 알림은
하나도 없다.**

멱등성과 중복 알림 차단도 그대로다.

```
재처리 1회차 후 행 증가 : 0
재처리 2회차 후 행 증가 : 0
저장 로그 증가(신규)    : 0
중복 로그 증가(건너뜀)  : 12
행 수 == 고유 event_id  : 16,863 == 16,863
```

---

## 5분 장애로 다시 — 버그 하나와 오해 하나를 찾았다

"PostgreSQL 장애가 `max.poll.interval.ms`(300초)를 넘으면 리밸런싱이 도는가"가
미측정으로 남아 있었다. 장애를 **300초**로 늘려 돌렸다.

### 발견 1 — 내가 만든 버그: 변환 단계가 트랜잭션을 열고 있었다

첫 300초 실행에서 로그에 **"배치 저장 실패 0건"**이 반복해서 찍혔다.
저장할 것이 하나도 없는데 저장이 실패한다는 뜻이라 앞뒤가 안 맞았다.

원인은 `AnomalyService`의 **클래스 레벨 `@Transactional(readOnly = true)`**였다.
배치화하면서 파싱을 트랜잭션 밖으로 빼려고 `toEntity()`를 분리했는데,
클래스 레벨 애너테이션 때문에 **그 메서드도 트랜잭션을 열고 있었다.**
DB가 죽어 있으면 `Could not open JPA EntityManager`로 **변환 단계에서** 실패하고,
컨슈머는 그것을 "이 메시지가 잘못됐다"로 오해해 레코드를 하나씩 DLQ로 보냈다 —
배치화로 없애려던 바로 그 동작이다.

그리고 그 뒤 `saveAll([])`이 빈 리스트에도 트랜잭션을 열어 또 실패했고,
offset이 커밋되지 않아 같은 배치가 계속 되돌아왔다. 그래서 DLQ에는
**8건의 알림이 19개 레코드로** 불어나 있었다(고유 event_id 8개, 레코드 19개).

유실은 없었다 — PostgreSQL이 살아난 뒤 미커밋 배치가 다시 처리돼 전부 저장됐다
(행 40,338 = 토픽 고유 event_id 40,338). 하지만 DLQ가 실제보다 부풀려 보였다.

고친 것: `toEntity()`에 `@Transactional(propagation = NOT_SUPPORTED)`,
`saveAll()`에 빈 배치 early return. 회귀 테스트 2건 추가.

### 발견 2 — "180초 예산"은 벽시계 시간이 아니다

고친 뒤 같은 300초 장애를 다시 돌렸다.

| | 수정 전 | 수정 후 |
| --- | ---: | ---: |
| 장애 중 DLQ | 19 (고유 8) | **0** |
| "배치 저장 실패 0건" 로그 | 반복 | **0** |
| 리밸런싱 로그 | 0 | **0** |
| 유실 | 0 | **0** (행 43,347 = 토픽 고유 43,347) |

**예산이 180초인데 300초 장애에서 DLQ가 0건이다.** 처음엔 이상해 보였는데,
`ExponentialBackOff.setMaxElapsedTime`이 세는 것은 **백오프로 쉰 시간의 합**이지
벽시계 시간이 아니다. 로그를 보면 재시도가 약 30초 간격으로 24회 찍혀 있는데,
그 30초는 백오프가 아니라 **HikariCP `connectionTimeout`을 기다린 시간**이다.
백오프 합(1+2+4+8+16+30…)은 그동안 180초에 도달하지 못했다.

기본값 기준 실효 내성은 대략 **8분** 수준이다(10회 시도 × 30초 + 백오프 181초).
`CLAUDE.md`·Runbook·코드 주석에 "총 경과 시간 180초"라고 적어둔 것을 바로잡았다.

같은 이유로 "예산 &lt; `max.poll.interval.ms`"라는 관계도 그대로 성립하지 않는다.
컨슈머가 쫓겨나는 기준은 **poll 사이의 벽시계 시간**이라, 리스너가 오래 붙잡히면
예산과 무관하게 리밸런싱이 돌 수 있다. 300초 장애에서는 실측으로 0건이었지만,
**더 긴 장애에서 어디서 도는지는 재지 않았다.**

## 남은 것

- ~~PostgreSQL 장애 5분 초과 시 리밸런싱 미측정~~ — **쟀다**(위 절). 300초에서 0건.
  다만 **8분(실효 내성)을 넘는 장애는 여전히 미측정**이다.
- ~~HikariCP `connectionTimeout` 조정 여부~~ — **줄이지 않기로 결정**했다.
  근거는 `docs/runbook/dlq-reprocessing.md` 4-2절.
- ~~알림 경로가 재시도 예산을 안 탄다~~ — **바꿨다**(ADR-022). 텔레메트리와 같은 정책이다.

## 이 문서의 한계

- **각 조건 1회 실행이다.** in-doubt 건수(9건 중 3건, 19건 중 8건, 6건 중 6건)는
  장애 주입 타이밍에 좌우되는 값이라 **반복하면 달라진다.** 이 문서가 보이는 것은
  "in-doubt 건이 실제로 생긴다"이지 "몇 건 생긴다"가 아니다.
  `docs/roadmap.md` P0-2가 반복 측정을 요구한다.
- **원본 로그를 보존하지 않았다.** 스크립트가 `_result.txt`에 남긴 출력을 보고 이
  문서를 쓴 뒤 지웠다. 여기 적힌 수치를 원본에서 다시 계산할 수 없다
  (`docs/evidence-policy.md` P0-1 위반).
- **commit SHA는 실행 시점이 아니라 "그 결과를 낸 코드가 들어간 커밋"이다.**
- 단일 머신 결과다.
