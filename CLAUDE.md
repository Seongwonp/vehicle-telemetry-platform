# Vehicle Telemetry Platform — AI 협업 가이드

## 프로젝트 개요

차량(OBD-II 동글 또는 시뮬레이터)에서 발생하는 실시간 센서 데이터를 수집하고, 이상 감지 및 모니터링까지 처리하는 백엔드 플랫폼.

- **개발자**: Sungwon
- **목적**: 백엔드/서버 엔지니어 포트폴리오 (현대오토에버 등 모빌리티 IT 기업 지원)
- **개발 기간**: 약 10주

---

## 기술 스택

| 영역 | 기술 |
|------|------|
| 데이터 수신 | MQTT (Eclipse Mosquitto) |
| 메시지 큐 | Apache Kafka |
| 백엔드 API | Java 17 + Spring Boot 3 |
| 이상 감지 | Python 3.11 (룰 기반 + scikit-learn 선택) |
| 시계열 DB | InfluxDB |
| 관계형 DB | PostgreSQL |
| 캐시 | Redis |
| 모니터링 | Grafana + Prometheus |
| 차량 시뮬레이터 | Python 또는 C |
| 보안 | JWT, TLS/SSL, Rate Limiting, 이상 접근 감지 |
| 인프라 | Docker Compose, (선택) AWS EC2 |

---

## 디렉토리 구조

```
vehicle-telemetry-platform/
├── simulator/          # 차량 데이터 시뮬레이터 (Python/C)
├── broker/             # Mosquitto MQTT 브로커 설정
├── kafka/              # Kafka 설정 및 토픽 초기화 스크립트
├── backend/            # Spring Boot API 서버
│   ├── src/
│   └── build.gradle
├── anomaly-detector/   # Python 이상 감지 모듈
├── monitoring/         # Grafana + Prometheus 설정
├── docker-compose.yml
├── CLAUDE.md
└── README.md
```

---

## 개발 원칙

- **보안 우선**: 모든 통신은 TLS, 인증 없는 엔드포인트 금지
- **단계별 구현**: Phase 순서를 지켜서 개발 (파이프라인 → API → 이상감지 → 보안 → 배포)
- **실제 OBD-II 연동 고려**: 시뮬레이터와 실제 OBD-II 동글 전환이 쉽도록 인터페이스 분리
- **포트폴리오용**: 코드 가독성, README, 아키텍처 문서 품질 중요

## 작업 시작 전 기준 문서

Telemetrix는 기능 수보다 차량 데이터의 유실, 중복, 순서, 장애 격리, 복구와 관측
가능성을 실제 테스트로 증명하는 것을 우선한다. 새 작업을 시작하기 전에 아래 문서를
확인한다.

- 우선순위와 의도적으로 미루는 작업: `docs/roadmap.md`
- GitHub Issue 작성 및 종료 기준: `docs/issue-guidelines.md`
- 주장에 필요한 증거와 검증 상태 표기: `docs/evidence-policy.md`

Kubernetes, 서비스 추가 분리, 신규 저장소나 메시징 기술은 현재 문제와 측정 근거가
있을 때만 검토한다. 한 번의 성공 결과를 일반화하지 않고, 확인하지 못한 항목은
`미검증`으로 남긴다.

## 작업을 끝낼 때 — 문서를 여기까지 맞추고 끝낸다

**작업이 끝난 시점은 코드가 도는 시점이 아니라 문서가 따라잡은 시점이다.**
측정 결과가 문서에 없으면 다음 세션이 같은 것을 다시 재고, 판단 근거가 없으면
같은 결정을 다시 논쟁하게 된다. 아래를 다 채우고 커밋한다.

| 대상 | 무엇을 |
| --- | --- |
| `load-test/<scenario>/RESULT_*.md` | 검증 상태·적용 범위·코드 상태·실행 명령·원본 경로 헤더, 결과, **"이 문서의 한계"** |
| `evidence/<run-id>/` | 스크립트가 자동 저장한다. **지우지 마라** — 2026-09-05에 임시 파일로 알고 지워서 그날 수치를 재계산할 수 없게 만든 적이 있다 |
| `docs/roadmap.md` | 해당 항목에 완료 표시와 **남은 갈래**. 새로 생긴 미검증 항목도 여기로 |
| `docs/architecture-decisions.md` | 동작을 바꾸는 결정을 했다면 ADR. 안 바꾸기로 한 결정도 근거와 함께 |
| `CLAUDE.md` 최상단 | 다음 세션이 처음 읽는 곳 — 닫은 것, 미결 판단, 다음 후보 |
| `README.md` | 포트폴리오 독자가 보는 곳. 수치에는 **적용 범위**를 같이 쓴다 |
| `docs/devlog.md` | 그날의 서술 기록 — **결과보다 틀린 지점**을 남긴다 |

`docs/evidence-policy.md`의 표현 규칙을 지킨다. 1회 실행은 "그 조건에서 관찰됨"이고,
안정성 주장에는 3회 이상이 필요하다. 실패한 시도와 측정 도구의 한계도 지우지 않는다.

**devlog는 밀리기 쉽다.** 결과 문서가 "무엇을 결론지었나"를 남긴다면 devlog는
"어디서 틀렸고 무엇이 그것을 잡았나"를 남긴다. 이 프로젝트에서 반복해서 값을 낸 것은
후자였다 — 되돌아보면 유실을 찾은 계기가 대부분 "숫자가 앞뒤가 안 맞는 순간"이었고,
그건 결과 표에는 안 남는다.

## 현재 최우선 작업 (2026-09-09)

> **2026-09-09 — 대조군이 없으면 근거도 없다.**
> 정적 멤버십은 12시간 soak test의 리밸런싱 폭풍을 피하려고 켠 것이고 근거가 분명해서
> 의심한 적이 없었다. 그런데 어제 "스케일 다운 45초는 정적 멤버십 때문"이라고 쓸 때
> **그것도 설명이지 측정이 아니었다.**
>
> 끄고 재보려니 **끌 수가 없었다** — Kafka는 `group.instance.id`에 빈 문자열을 허용하지
> 않아(`NonEmptyString`) 기동이 실패한다. yml에서 줄을 지우고 재빌드하는 수밖에 없었다.
> `KafkaConfig.staticMembershipOffWhenBlank`로 **빈 값이면 속성을 제거**하게 했다(ADR-024).
> compose는 `${VAR-기본값}`으로 받는다 — **`:-`면 빈 값이 기본값으로 되돌아가** 끄려는
> 의도가 조용히 무시된다.
>
> | | 정적 멤버십 ON (3회) | OFF (1회) |
> | --- | ---: | ---: |
> | 멤버 9 → 3 | 46 / 52 / 54초 | **9초** |
> | 정지 파티션 | **6개** | **0개** |
>
> **끄면 컨테이너가 멈추는 즉시 재할당된다.** 설명이 측정이 됐다.
> **다만 기본값은 안 바꿨다** — 이 대조군은 **끄는 것의 이득만** 봤고 켠 이유(폭풍)는
> 재현하지 않았다. 한쪽만 재고 뒤집는 것은 2026-09-07에 "약 57,000"으로 한 실수와 같다.
>
> **`session.timeout.ms`를 줄였을 때의 대가도 쟀다**(어제 남긴 미검증).
> `docker pause`로 **같은 20초 정지**를 주입하니 **45,000ms는 리밸런싱 0회**(멤버 6 유지),
> **10,000ms는 2회**(정지 중 멤버 6 → 3)다. 이걸로 양쪽이 다 숫자가 됐다 —
> 유지 비용은 스케일 다운 46~54초, 줄이는 비용은 20초 순단마다 리밸런싱 2회.
> **스케일 다운은 시간을 고를 수 있지만 GC 정지·순단은 고를 수 없어 45,000ms를 유지한다.**
> 실험에서 제일 신경 쓴 건 **설정이 안 먹은 채 "차이 없음"이 나오는 것**이라,
> 조건마다 backend 로그에서 실제 컨슈머 설정을 읽어 대조하고 안 맞으면 중단하게 했다.
>
> **`dtc_codes`(배열)에서 조용한 변형을 둘 더 찾았다.** `toPoint()`가
> `String.join(",", ...)`으로 배열을 접는데, **`[null]`은 `"null"` 네 글자가 되고**
> **쉼표가 든 코드는 구분자와 충돌**해 코드 1개인지 2개인지 구분할 수 없게 된다.
> 둘 다 저장되고 신호가 0이다. **고치지 않기로 했다** — 값이 사라지지 않고(DTC 형식에
> 안 맞아 눈에 띈다) 쉼표는 스펙을 지키면 못 생기며 이상 감지는 원본 JSON을 본다.
> **다시 볼 조건**(실제 장치에서 한 번이라도 관찰되면 뒤집는다)을 코드 주석에 남겼다.
>
> **부하 중 스케일 다운은 3회를 채웠다** — 재할당 46/52/54초, 정지 파티션 6개,
> **유실 0은 3/3**. 전체 테스트 **118개 통과, skip 0**.
>
> **다음 후보**: 앱 **실기기 검증**(기기 필요 — 유일하게 남은 P1),
> **정적 멤버십을 끄는 것의 대가**(리밸런싱 폭풍 재현 — ADR-024의 가장 큰 공백,
> `max.poll.interval.ms` 초과를 주입해야 하고 회당 5분 이상),
> 처리량 측정 재설계(회차 사이 휴식 + 구간 순서 섞기 + CPU 온도·클럭),
> 끈 쪽 대조군 3회 채우기, 정지 길이를 바꿔가며 세션 타임아웃 경계 찾기.
>
> 서술 기록은 `docs/devlog.md`의 2026-09-09 항목.
>
> ---
>
> **2026-09-08 — 측정한 동작이 문서에 없는 기본값에 매달려 있었다.**
> 어제 "스케일 다운은 재할당까지 44초"를 재서 Runbook에 적었는데, 그 45초를 만드는
> `session.timeout.ms`가 **우리 설정 어디에도 없었다**(Kafka 클라이언트 기본값).
> P0-3의 반대 방향이다 — 그때는 **설정이 있는데 동작이 없었고**, 이번엔
> **동작이 있는데 설정이 없었다.** 클라이언트를 올리다 기본값이 바뀌면 Runbook이
> 조용히 틀린 말이 된다.
> **같은 값(45,000ms)을 명시**하고 `KAFKA_SESSION_TIMEOUT_MS`로 노출했다 —
> **동작은 하나도 안 바뀐다. 그게 요점이다.**
>
> 테스트를 어디에 걸지에서 한 번 갈렸다. `withPropertyValues`로 값을 주면 **배선만
> 증명한다** — yml에서 줄을 지워도 통과한다. 그래서 `KafkaConsumerContractTest`는
> `YamlPropertySourceLoader`로 **실제 `application.yml`을 읽어서** 단언한다.
> 무는지도 두 방향으로 확인했다: 기대값을 45001로 → 1건 실패, yml에서 줄 삭제 → 2건 실패.
> 전체 스위트 **116개 통과, skip 0**(Docker가 올라와 있어 Testcontainers 계약 5종도 실제 실행).
>
> **값은 안 바꿨다.** 줄이면 스케일 다운이 빨라지지만 GC 정지·순단을 죽음으로 오판해
> 리밸런싱이 는다 — 정적 멤버십은 그 폭풍을 피하려고 켠 것이다.
> **줄였을 때의 대가는 아직 안 쟀다.**
>
> **같은 날 dev(평문) 프로파일에서 `scale`을 실기동했다**(어제 남긴 미검증).
> 구조는 기본(mTLS) 프로파일과 같다 — 멤버 9 / 정적 id 9 / 호스트 3 / 할당 파티션 9,
> fencing 0, 브로커 접속은 주 backend 하나. dev 오버레이가 저장 인스턴스에도 먹어
> `MQTT_PORT=1883`·`MQTT_TLS_ENABLED=false`가 들어간다. 위에서 명시한
> `session.timeout.ms = 45000`도 실제 컨슈머 설정에 찍혔다.
>
> **여기서 어제 안 보이던 것을 하나 봤다 — 파티션을 늘려도 컨슈머는 최대 5분간 못 본다.**
> 어제는 backend 기동 **전에** 파티션을 늘렸고 오늘은 순서가 반대였다. 그랬더니
> 리밸런싱은 즉시 도는데 **할당은 3개 그대로**였다(파티션은 9개인데).
> `metadata.max.age.ms`(기본 5분)가 지나야 새 파티션이 보인다 — 실측 4분 27초 뒤
> 재리밸런싱으로 9개가 됐다. 운영에서는 정확히 **"인스턴스를 늘렸는데 처리량이 그대로"**
> 로 보인다. Runbook에 순서와 대기 시간을 넣었다.
>
> **그리고 내 스크립트가 정상을 장애처럼 보여주고 있었다.** `status`의 Prometheus 확인이
> `curl | grep || echo "(응답 없음)"` 모양이라 **grep 0건 = 응답 없음**으로 찍혔다
> (실제로는 200에 대상 2개, file_sd 갱신 주기 30초를 아직 안 지난 것뿐).
> `static_configs`를 피한 이유를 스크립트 메시지에서 스스로 어긴 셈이라 고쳤다.
>
> 스케일 다운은 **2회차 46초**(1회차 44초). 프로파일이 달라도 같은 자리다.
> **아직 2회라 안정성 주장은 못 한다.**
> `docs/verification/2026-09-08-dev-profile-scale.md`.
>
> **같은 날 NaN·음의 Infinity도 주입했다**(2026-09-06에 남긴 미검증).
> `-1e309`는 예상대로 `toPoint()`의 `finite()`에 걸린다 — 양수와 **같은 예외·같은
> `permanent` 분류**. **NaN은 그 검사까지 오지도 않는다** — JSON 리터럴이 아니라
> Boot 기본 `ObjectMapper`가 **역직렬화 단계에서 거부**한다.
> 결과만 보면 "NaN도 막힌다"가 맞지만 **막는 주체가 내가 넣은 방어가 아니다.**
> `finite()`의 NaN 분기는 지금 입구에 대해서는 **잉여**이고, 산술로 NaN이 생기는 경로가
> 들어오면 그때 유효해진다 — 나중에 이 방어를 정리하려는 사람이 판단할 근거라
> 결과 문서와 javadoc 양쪽에 적었다. 대조군은 두 유형 모두 100/100.
> `load-test/poison-message/RESULT_20260908_nan_neginf.md`.
>
> **스케일 다운 3회도 채웠다** — dev 프로파일 동일 조건 **46/48/47초**(폭 2초, 4.3%).
> 이제 "약 45~48초"라고 쓸 수 있다(mTLS 1회는 44초). `down` 반환에 4~5초가 들어가므로
> **순수 세션 만료는 42~43초**로 `session.timeout.ms`(45,000ms)와 같은 자리다.
> **회차 하나를 버렸다**: 처음 잰 값이 8초로 나왔는데, 스케일 업 대기 조건을
> `할당 파티션 >= 9`로만 걸어둬서 **기존 3개 멤버가 이미 9개를 쥔 상태**에 즉시 통과했고
> 저장 인스턴스가 붙기도 전에 내린 것이었다. **멤버 수와 할당 수를 둘 다 봐야 한다.**
> 값이 범위 밖으로 튀어준 덕에 잡혔다.
>
> **CLAUDE.md를 내가 한 번 망가뜨렸다** — `9db93bf`에서 awk 편집이 맨 위 제목을 날리고
> 이 블록을 파일 상단에 통째로 중복 복사했다. 같은 날 복구했다.
> **awk로 파일을 재조립할 때는 편집 후 앞부분을 원본과 대조한다.**
>
> **`rpm`(정수 필드) 경계도 봤다 — 조용한 값 변형을 하나 더 찾았다.**
> 범위 초과와 `1e309`는 **역직렬화**에서 DLQ다(같은 `1e309`가 `speed`에서는 `finite()`까지
> 온다 — **막는 문이 타입에 따라 다르다**). 그런데 **`rpm: 2000.7`은 아무 데서도 안 걸리고
> 2000으로 잘려 저장된다**(DLQ·에러·카운터 0, 행 수도 맞아 정합성 대조로도 안 잡힘).
> **고치지 않기로 했다** — 손실이 1 rpm 미만이고, 이상 감지는 `rules.py`가 원본 JSON을
> 보므로 영향이 없다. 다만 **저장 데이터로 룰을 다시 돌리면 6000.9가 6000이라 안 걸린다.**
> `load-test/poison-message/RESULT_20260908_rpm_int.md`.
>
> **부하 중 스케일 다운도 쟀다(2회).** Runbook의 "지연이 튄다"는 추론이었다.
> 제거된 인스턴스가 쥔 **6개 파티션이 완전히 정지**하고(평균 lag 7,137 → 177,952)
> **살아있는 backend의 3개는 평평하다**(2,244 → 1,324). 격리는 성립하고 대가는 그 6개다.
> 재할당은 무부하 때와 같은 46~52초, **유실 0**(토픽 = 행, 2회 모두 일치).
>
> **여기서 내 계측이 먼저 함정에 걸렸다.** "무소유 파티션 수"를 지표로 넣었는데 끝까지
> **0**이었다 — **정적 멤버십은 죽은 멤버가 세션 만료까지 소유권을 유지한다.**
> `--describe`만 보면 9개 다 주인이 있어 멀쩡해 보인다.
> **"소유자가 있다"는 "소비되고 있다"를 뜻하지 않는다.** 지표를 `current_offset` 정지로
> 바꾸니 2회차에서 정확히 6개가 잡혔다. 운영에서도 같은 함정이라 Runbook에 경고를 넣었다.
> `load-test/storage-scale/RESULT_20260908_scaledown_under_load.md`.
>
> **다음 후보**: 앱 **실기기 검증**(기기 필요 — 유일하게 남은 P1),
> 부하 중 스케일 다운 3회차(지금 2회), 처리량 측정 재설계(회차 사이 휴식 + 구간 순서 섞기 +
> CPU 온도·클럭), 정적 멤버십 끈 대조군, `session.timeout.ms`를 줄였을 때의 대가 측정,
> `dtc_codes`(배열) 독성 주입.
>
> 서술 기록은 `docs/devlog.md`의 2026-09-08 항목.
>
> ---
>
> **P0 전체(1·2·3)와 P1-1·P1-2를 닫았다(2026-09-05~06).**
> - **P0-3**: `application.yml`의 `spring.kafka.listener.*`가 **실제로는 무효**였다
>   (커스텀 팩토리가 Boot 설정을 안 물려받고 Java에 하드코딩). configurer로 고치고
>   `KafkaListenerContractTest`로 고정. README 아키텍처 다이어그램 3곳도 실제와 달랐다.
> - **P1-1**: Kafka 장애 후 spool 드레인에서 **역전 924건, 최대 114초 과거**.
>   저장·조회는 영향 없고 앱 실시간 화면만 영향 — 앱에서 막았다(`7f5002b`).
> - **P1-2**: 독성 5유형 중 정상 레코드를 함께 막은 것은 없다. 다만
>   **`speed: 1e309`는 저장되면서 그 필드만 조용히 사라진다**(DLQ·에러·카운터 전부 0).
>
> **P0-1(증거 보존)과 P0-2(3회 반복)도 닫았다.**
> 실행마다 `evidence/<run-id>/`에 원본이 남고, 핵심 실험 셋은 각 3회 반복해
> 변동 폭을 냈다 — `load-test/*/RESULT_20260905_*repeat.md`.
> 반복이 **잘못된 서술 두 개를 잡았다**: 리밸런싱 "재전달 40건"은 42/6/11로 7배
> 흔들렸고, 강제 종료 "재전달 68건"은 측정 자체가 불가능한 값이었다.
>
> 아래 본문의 나머지 수치는 여전히 **각 조건 1회**다(`부분 검증`).
>
> **P1-3·P1-4·P1-5도 닫았다(2026-09-06 오전).**
> - **P1-3**: 같은 720초 장애를 의존성만 바꿔 주입해 **재시도 예산이 소진되는 지점을
>   처음 봤다** — InfluxDB 220초, PostgreSQL 485초. "같은 180초가 3.7분과 8.1분이 된다"가
>   추정에서 측정이 됐다. 리밸런싱 0, 유실 0. **DLQ 발행이 로그에 안 남던 구멍**을 찾아
>   고쳤다(알림 경로는 남기는데 저장 경로만 안 남았다).
> - **P1-4**: 앱 저장소에 CI 추가(`vehicle-telemetry-app@6adca8a`), 첫 실행 success.
>   **실기기 검증은 여전히 남았다**(기기 필요).
> - **P1-5**: 표식 차량으로 개별 레코드를 5단계에서 확인 — 20/20 도달,
>   WebSocket 지연 p50 536ms.
>
> **P1을 닫으며 생긴 미결정도 같은 날 정리했다(P1.5).**
> - **P1.5-1**: `toPoint()`에 유한값 검사. `speed: 1e309`가 **저장(필드 소실) → DLQ 1건**이
>   됐다. 새 예외가 DLQ 분류에서 `unknown`이던 것도 고쳤다.
> - **P1.5-2**: 리밸런싱이 안 도는 이유는 **정적 멤버십이 아니라 에러 핸들러**였다 —
>   `ErrorHandlingUtils.retryBatch`가 파티션을 pause한 채 `poll(0)`을 계속 호출한다
>   (jar 바이트코드로 확인). 코드 주석의 반대 서술을 정정. 720초 장애 2회차도 겸했다.
> - **P1.5-3**: 장애 중 추적에서 **추적 도구가 먼저 죽었다**(끊긴 단계를 조회하다
>   ConnectionError). 고친 뒤 마커 20건 전부 `최초 미도달 단계=influx`로 갈린다.
> - **P1.5-4**: 죽은 토픽 `vehicle-dtc-events` 제거.
>
> **저장 경로 수평 확장도 쟀다(2026-09-06 오후, P2 항목).**
> 2026-09-05에 "스케일이 안 돼 미측정"으로 남겼던 것이다. 막고 있던 건 **고정된 식별자
> 둘** — MQTT `client-id`(`cleanSession=false`)와 Kafka `group.instance.id`. 그대로
> 복제하면 브로커가 앞 세션을 끊고 정적 멤버가 서로를 밀어내서 **멤버가 3에서 안 는다**
> (fencing 45건, 연결 끊김 44건). `mqtt.ingest.enabled` 스위치로 **수집 1 + 저장 N**으로
> 나누니 멤버 **9**, fencing 0, PUBACK=토픽=행 **117,125**로 일치.
> **처리량 이득은 미측정**(파티션 3개라 6개는 유휴, 이 부하엔 1대로 충분).
>
> **데이터 보존·삭제 정책도 정했다(2026-09-06 밤, P2).**
> 설정을 읽어보니 **InfluxDB 버킷 보존이 무기한**이었다(`DOCKER_INFLUXDB_INIT_RETENTION`
> 미설정 → InfluxDB 2.x 기본이 무제한). GPS가 담긴 텔레메트리가 영구히 쌓이고 있었다.
> **90일**로 정했다(90일 넘겨 조회하는 기능이 하나도 없다). `backend` 로그 로테이션도 추가
> (요청 로그에 `clientIp`). 차량 삭제는 소프트 삭제 유지 — 이유와 수동 삭제 절차는
> `docs/data-retention.md`.
> **주의**: `DOCKER_INFLUXDB_INIT_*`는 버킷 최초 생성에만 적용된다. 기존 환경은
> `influx bucket update`가 필요하고 그 마이그레이션 절차는 아직 없다.
>
> **처리량 이득도 쟀다(2026-09-06 저녁, P2).** 오후에 "미측정"으로 남긴 것이다.
> 파티션을 12로 올리고 Kafka에 직접 계속 발행해 A/B/A로 쟀다 —
> **1개 19,428 → 3개 50,894 msg/s**, A 표본(≤31,047)과 B 표본(≥33,935)의 **범위가
> 겹치지 않는다.** 다만 **배수는 부하 조건의 함수다**: 발행 33~40k에서 1.34배,
> 78~92k에서 2.62배. 같은 1 인스턴스인 A와 A'도 27% 차이 난다 — **단일 배수로 인용하지 마라.**
> 결과: `load-test/storage-scale/RESULT_20260906_throughput.md`.
>
> **이날 내가 두 번 크게 틀렸다(같은 문서 5절, devlog에 서술).**
> (1) 실험 전 정리 명령 `rm -rf evidence/20260906-16*`이 **실행 중이던 실험의 증거를 지웠다**
> — 이 파일 위에 적어둔 경고를 그대로 반복했다. **evidence를 글롭으로 지우지 않는다.**
> (2) 재실행이 `BACKLOG: unbound variable`로 조용히 죽었는데, 로그 감시자가 **앞선 실행의
> 이벤트를 늦게 전달**하는 바람에 그걸 재실행 결과로 읽고 "재현됐다"는 표까지 썼다.
> 잡아낸 건 증거 파일을 직접 연 것이었다. **결과는 이벤트가 아니라 증거 파일에서 읽는다.**
>
> ---
>
> ## 2026-09-07에 한 것 — 상한을 못 찾았고, 보존 삭제는 되돌릴 수 없다
>
> **0-Z. 3회 반복 결과 — 이 환경에서는 천장을 숫자로 말할 수 없다.**
> 같은 조건 3회에서 **모든 인스턴스 수가 회차마다 단조 감소**했다(무작위 변동이 아니다).
> 8 인스턴스: **56,985 → 44,279 → 26,564 (2.15배 차)**.
> **3회차에서는 8개가 4개보다 20% 느렸다**(26,564 < 33,323) — 1·2회차와 반대다.
> 원인은 **못 밝혔다**. 디스크(622GB 여유)도 호스트 CPU(최대 924%/1600%)도 아니다.
> 후보는 **약 2시간 30분 연속 고부하에 따른 발열 스로틀링**인데 온도를 안 쟀다.
> **회차 간 비교가 성립하지 않는다** — 그래서 오늘 수치는 전부 "그 시점 관찰"이다.
> **살아남는 것은 방향뿐**: 인스턴스를 늘리면 쓰기 지연이 늘고 처리량 증가는 줄어든다.
> 결과: `load-test/storage-scale/RESULT_20260907_repeat3.md`,
> 회차 표: `REPEAT_20260907_120951.md`.
>
> **다음에 이 측정을 하면**: (1) 회차 사이에 쉬어라(연속이면 뒤 회차가 불리하다),
> (2) **구간 순서를 섞어라** — 지금은 항상 1→2→4→8이라 인스턴스 수와 경과 시간이
> 같이 움직인다, (3) CPU 온도·클럭을 같이 찍어 발열 가설을 확인하거나 버려라,
> (4) A/A' 대조는 반드시 유지 — 3회차 드리프트를 잡은 유일한 내부 증거였다.
>
> **도구도 둘 고쳤다.**
> (1) `summarize.sh`의 `$((mx - mn))`이 정수 산술이라 **소수 지표에서 문서 생성이
> 조용히 끊겼다**(표가 한 줄에서 끝남). awk로 교체.
> (2) **중단된 실행이 정상 실행과 구분되지 않았다** — 오늘 다섯 번 죽었는데 남은
> 증거 폴더가 완주와 겉모습이 같아서, 첫 집계가 "4회 반복 → 검증 완료"로 나왔다
> (진짜 완주 3회). 이제 `evidence_init`이 `status.txt`에 RUNNING을 쓰고
> `evidence_finish`가 COMPLETE로 바꾼다. `summarize.sh`는 완주만 세고 중단은
> 별도 표에 남긴다(지우지 않는다). **`status.txt`가 없는 옛 실행은 완주로 본다.**
>
> **오늘 조용한 실패가 셋**이었고(`BACKLOG` 미정의, actuator 긁기의 루프 정지,
> `summarize.sh` 정수 산술) 전부 `set -euo pipefail`이나 리다이렉트와 겹쳐 에러가
> 안 보였다. **셋 다 결과 파일을 직접 열어서 잡았다.**
>
> **0-A. 천장의 원인 — InfluxDB 쓰기 지연. 그리고 "약 57,000"은 정정한다.**
> `TelemetryRepository`에 이미 있던 `telemetry.influx.write` Timer를 구간별로 뽑았다.
> **배치 크기가 2,000으로 고정인데 쓰기 지연만 늘어난다**:
> 1개 388ms → 2개 378ms → **4개 665ms** → 8개 717ms.
> **1→2에서는 지연이 그대로고 처리량이 2배**가 되는데(진짜 병렬 이득),
> **2→4에서 지연이 76% 뛰면서 처리량 증가가 +17%로 꺾인다.** 이게 천장의 메커니즘이다.
> 호스트 CPU는 864%/1600%로 여전히 절반이다 — CPU가 아니라 I/O 대기가 맞다.
>
> **동시에 "약 57,000"은 취소한다.** 같은 조건 2회가 56,985와 **44,279**로 25% 벌어진다.
> 1회차에서 두 구성이 1.5% 안에 든 것은 우연에 가까웠다.
> **살아남는 결론은 숫자가 아니라 모양이다** — 수확 체감(+125%/+17%/+17%)과 그 원인.
> 천장을 숫자로 말하려면 3회 이상 필요하다.
> 결과: `load-test/storage-scale/RESULT_20260907_writelatency.md`.
>
> **계측 구조도 바꿨다**: actuator 긁기를 `sample()` 안에서 하다가 **측정 루프가 3분간
> 멈췄다**(curl은 손으로 재면 97ms인데도). 원인 미상. **별도 프로세스로 분리**해서
> 감시자가 멈춰도 스윕이 도는 구조로 바꿨다. **계측을 추가할 때는 그것이 실패해도
> 측정이 계속되는지부터 봐라** — 이걸 안 봐서 25분짜리 실행 하나를 버렸다.
>
> **0-B. (그 전 단계) 천장의 존재와 CPU 배제.**
> 파티션 24 · 인스턴스 8까지 다시 훑으니 **수확 체감이 드러났다**
> (21,849 → 35,304 → 46,633 → 56,985 = +62%/+32%/+22%). 결정적인 건
> **서로 다른 두 구성이 같은 값에 수렴한 것**이다 — 파티션 12·인스턴스 4에서 57,864,
> 파티션 24·인스턴스 8에서 56,985로 **1.5% 차이**.
> **CPU는 범인이 아니다**: 호스트 CPU 합 923.7% / 1600%(58%)이고, backend CPU 합이
> 4개→8개에서 **142.0 → 146.8로 거의 안 늘었다**(인스턴스는 2배) — 새 컨슈머가 일하는 게
> 아니라 **기다린다**. 병목은 컨슈머 뒤(InfluxDB 쓰기 또는 디스크 I/O)에 있다.
> **다음엔 CPU 말고 InfluxDB 쓰기 지연·디스크 IOPS를 재라.**
> 결과: `load-test/storage-scale/RESULT_20260907_sweep24.md`.
>
> **측정 도구의 한계도 확인했다**: 컨테이너별 InfluxDB CPU는 같은 조건에서 8배까지
> 흔들린다(원본 표본 0.23%~144%). `docker stats --no-stream`의 짧은 창과 배치성 쓰기가
> 겹쳐서다. **컨테이너별 CPU 중앙값은 쓰지 마라 — 호스트 합만 유효하다.**
>
> **1. (그 전 단계) 파티션 12 스윕에서는 상한을 못 찾았다.**
> 인스턴스를 1→2→3→4로 훑고 컨테이너별 CPU를 같이 찍었다.
> **24,390 → 40,470 → 44,771 → 57,864 msg/s로 꺾이는 지점이 없고**, InfluxDB CPU도
> 49% → 153%로 함께 오른다. **어제 1순위 용의자였던 InfluxDB 포화는 이 범위에서 상한이 아니다.**
> 기준선 A'/A = 1.09(어제 1.27)로 안정적.
> **4개에서 멈춘 건 임의가 아니다** — `concurrency=3`이라 4개(12스레드)가 파티션 12개를
> 정확히 채운다(멤버 3→6→9→12 확인). 어제 "6개도 재자"고 적은 건 이 계산을 안 한 잘못이었다.
> 다음엔 **파티션 24 + 인스턴스 8개**, 그리고 **호스트 전체 CPU**를 같이 재야 한다
> (지금은 컨테이너별 값만 있어 호스트 포화를 판정할 수 없다).
> 결과: `load-test/storage-scale/RESULT_20260907_sweep.md`.
>
> **2. InfluxDB 보존 마이그레이션 절차를 만들고 실측했다.**
> `docs/data-retention.md`에 미해결로 남아 있던 항목이다. 알아낸 것 셋:
> **(a) 삭제는 설정 변경 즉시가 아니라 검사 주기마다 일어난다** — 실험(60초 주기)에서 t+65초.
> **운영 기본값은 30분**이라 그동안 초과 데이터가 남는다. **(b) 삭제 중 쓰기는 안 막힌다**(실패 0건).
> **(c) 되돌릴 수 없다** — 무기한으로 되돌려도 0행. **일방향 작업이다.**
> Runbook: `docs/runbook/influxdb-retention-change.md`,
> 결과: `load-test/retention-migration/RESULT_20260907_migration.md`.
>
> **3. 도구가 다섯 번 조용히 죽었고, 그게 이날의 진짜 교훈이다.**
> `influx bucket update`는 **`--org`를 안 받고**(list·write·query는 받는다),
> **`--name`은 대상 선택이 아니라 새 이름 설정**이라 `--id`가 필요하다.
> Git Bash가 컨테이너 경로를 Windows 경로로 바꾸는데 `MSYS_NO_PATHCONV=1`을 붙이면
> 이번엔 호스트 경로까지 안 바뀐다(→ stdin으로 우회).
> `| head -N`이 SIGPIPE를 내고, Flux의 `range(stop: now)`는 400 오류다(`now()`여야 한다).
> **다섯 다 `set -euo pipefail`과 겹쳐 로그 한 줄 없이 끝났다.**
> → **실행 직후 증거 디렉터리와 로그가 진행되는지 확인하고 넘어간다.**
>
> **4. 어제 견적도 틀렸다.** "split을 compose에 반영하려면 스크립트 수십 개"라고 썼는데
> **증거 파일과 결과 텍스트를 같이 센 값**이었다(soak 로그 하나에 916건). 실행 코드만 세면
> **17개 파일**이고 반나절 규모다.
>
> **5. 수집/저장 분리를 compose로 옮겼다(저녁, 로드맵 P2 항목).**
> 부하를 안 거는 일을 골랐다 — 오늘 결론이 "부하 측정은 당분간 값이 안 나온다"였다.
> 2026-09-06에 만든 분리가 **실험 스크립트의 `docker run` 안에만** 있던 것을
> 프로파일 `scale`(`backend-storage-1..3`)로 옮겼다.
> **환경변수는 YAML 앵커 하나(`x-backend-env`)를 셋이 공유한다** — 복제하면 언젠가
> 갈라지는데 그건 측정에서 안 드러난다(둘 다 정상 동작하고 숫자만 달라진다).
> `docker compose config`로 렌더링해 **차이가 의도한 두 키뿐**이고 기존 `backend`는
> **바이트 단위 동일**임을 확인했다.
> Prometheus는 `file_sd_configs` — `static_configs`로 고정하면 프로파일을 안 켠 동안
> 계속 `up == 0`이고 그건 **장애처럼 보이는 정상**이다. 대상 파일은
> `scripts/scale-storage.sh`가 인스턴스와 함께 만들고 지운다.
> 구조 검증: 인스턴스 3개(수집 1 + 저장 2)가 파티션 9개를 정확히 3개씩, 정적 id 9종
> 무충돌, fencing 0, MQTT 세션은 주 backend 하나.
>
> **여기서도 대가를 하나 새로 쟀다 — 스케일 다운은 약 45초짜리다.**
> `down`은 3초에 반환하는데 컨슈머 그룹 재할당까지 **44초**(1회 관찰). 정적 멤버십이라
> 컨슈머가 종료해도 `session.timeout.ms`(클라이언트 기본 45초)를 기다린다. 그동안 그
> 파티션은 소비되지 않는다(유실 아님, 지연). **늘리는 건 싸고 줄이는 건 45초짜리다.**
> **주의**: 45초는 우리가 정한 계약이 아니라 **클라이언트 기본값**이다(미설정).
> 그리고 **처리량 이득은 이 변경으로 재지 않았다.**
> ADR-023, `docs/runbook/storage-scale-out.md`,
> `docs/verification/2026-09-07-compose-scale-profile.md`.
>
> **오늘의 총평 — 값을 낸 문장은 전부 부정형이었다.**
> 상한을 못 찾았고, 내가 쓴 "약 57,000"을 내가 취소했고, 3회를 채우니 그 3회가
> 회차 간 비교 자체를 무효로 만들었다. **오늘 한 일의 절반은 어제와 오늘 오전의 내
> 주장을 무너뜨린 것**이다. 남은 것은 방향 하나(인스턴스↑ → 쓰기 지연↑ → 증가폭↓)이고
> 크기는 이 환경에서 말할 수 없다. 그리고 도구 문제로 잃은 시간이 측정 시간보다 길었다.
> 저녁에 고른 compose 작업도 같은 모양으로 끝났다 — 되는 건 예상대로 됐고,
> **값이 나온 건 "줄이는 데 45초가 든다"는 대가 쪽**이었다.
>
> **다음 후보**(부하를 오래 거는 것은 당분간 값이 안 나온다 — 회차 간 비교가 깨진다):
> 1. ~~split 구성을 compose에 반영~~ — **오늘 저녁에 닫았다**(위 5번). 남은 갈래:
>    `session.timeout.ms`를 **설정으로 고정할지**(지금은 클라이언트 기본값 45초에 의존),
>    dev(평문) 프로파일에서 **실제 기동**(지금은 렌더링만 확인),
>    부하 실험 스크립트와의 **컨테이너 이름 충돌** 정리.
> 2. 앱 **실기기 검증**(기기 필요 — 유일하게 남은 P1 항목).
> 3. 처리량 측정 재설계: **회차 사이 휴식 + 구간 순서 섞기 + CPU 온도·클럭 계측**.
>    이 셋 없이 다시 재면 오늘과 같은 결과가 나온다.
> 4. 정적 멤버십을 끈 대조군, `FAULT=telemetry-kafka` 등 다른 단계에서의 추적,
>    3회 반복(장애 주입 쪽은 아직 대부분 1~2회), NaN·-Infinity 파이프라인 주입.
> 5. 보존 마이그레이션을 실제 운영 환경 절차로 옮길 때의 **검사 주기 대기**(기본 30분)
>    를 Runbook 체크리스트에 반영했는지 재확인.
>
> 서술 기록은 `docs/devlog.md`의 2026-09-07 항목.

**우선순위 1·2·3번을 모두 닫았다.**

- 1번 남은 갈래 둘 다 완료: (a) 밀리초 키 충돌을 재현했고(500건 → 1행) 임계값이
  차량당 1,000 msg/s, 현재 5 msg/s라 200배 여유여서 **고치지 않기로 판단**하고
  임계값을 코드에 남겼다. (b) 리밸런싱 재전달은 **40건(0.100%), 행 증가 0**.
- 2번 남은 갈래(이상 알림 DLQ 재처리 중복 검증): 행 중복 0. 대신 중복 *알림*을
  찾아 고쳤다.
- 3번 남은 갈래(MQTT 브로커 장애): **유실 129,445건(72.1%)을 찾아 0으로 고쳤다.**
  Paho 재연결 백오프 상한이 기본 128초였다.

추가로 **이상 알림 저장 경로를 배치화했다(ADR-022)** — 측정해보니 49 msg/s로
유입(193 msg/s)의 1/4만 처리하고 있었다. 배치 리스너 + 배치당 트랜잭션 1건으로
같은 부하의 lag이 **20,128 → 238**이 됐고, 4배 부하에서도 유입을 전량 소화한다.
DB 장애 시 DLQ 직행이던 것도 재시도(180초 예산)로 바꿔 텔레메트리 경로와 정책을 맞췄다.
같은 PostgreSQL 60초 장애를 다시 주입해 확인하니 **유실 6건 → 0건**이고,
DLQ에 남은 6건은 전부 이미 저장된 in-doubt 건이라 **복구가 필요한 알림이 하나도 없다.**
멱등성(재처리 2회에 행 증가 0)과 중복 알림 차단도 회귀 없이 유지된다.
결과: `load-test/anomaly-storage-throughput/`, `load-test/anomaly-dlq-idempotency/`.

PostgreSQL **300초** 장애도 재봤다 — DLQ 0건, 리밸런싱 0건, 유실 0건.
그 과정에서 배치화하며 내가 만든 버그(`toEntity()`가 트랜잭션을 열어 DB 장애 중
변환 단계에서 실패)를 찾아 고쳤고, **"180초 예산"이 벽시계가 아니라 백오프 합**임을
확인해 코드·문서 서술을 바로잡았다.

**남은 것은 대부분 실기기·실환경이 있어야 하는 것들이다.** 다음 후보:
7번(앱 실기기 검증 — 기기 필요), mTLS 프로파일에서 브로커 장애 재측정,
알림 저장 처리량 상한 확정(시뮬레이터를 다중 프로세스로 띄워야 잴 수 있다),
HikariCP `connectionTimeout` 30초 조정 여부.

---

## 이전 최우선 작업 (2026-09-04)

**Kafka 저장 실패 계약 검증 완료** — Docker가 있는 데스크톱에서 실행해
`KafkaStorageFailureContractTest` 3케이스가 skip 없이 통과했고(18.0초), 전체
`./gradlew test --no-daemon`도 96개 전부 통과(skip 0), CI(`e4effc2`)도 success다.
**5회 반복 실행에서도 5/5 통과라 간헐 실패(flakiness)는 관찰되지 않았다.**
실행 기록: `docs/verification/2026-09-04-desktop-docker-contracts.md`.

**우선순위 1번(강제 종료·재전달 시 InfluxDB 중복·덮어쓰기 수량 측정)도 완료** —
`docker kill` 후 재전달 68건(0.0873%)이 발생했으나 InfluxDB 행 수가 고유 키 수와
정확히 같아(77,929 = 77,929) **덮어쓰기로 흡수됨(멱등)**을 확인했다. 같은 밀리초
키 충돌은 이 부하에서 0건이라 ms 정밀도가 충분했다. 측정 도구와 절차는
`load-test/storage-integrity/`, 결과는 같은 폴더의 `RESULT_20260904_kill_redelivery.md`.

다음 작업은 아래 우선순위 2번부터다.

## 다음 우선순위

1. ~~consumer 강제 종료와 재전달 시 InfluxDB 중복·덮어쓰기 수량 측정~~ — **완료(2026-09-04)**.
   **같은 밀리초 키 충돌도 재현·판단 완료(2026-09-05)** — 같은 타임스탬프로 500건을
   보내면 **1행만 남는다**(499건이 에러·로그 없이 소실). 다만 임계값이 **차량당
   1,000 msg/s**(밀리초당 1건)이고 실측 유실률은 500→0.02% / 1,000→0.64% / 2,000→50.20%다.
   이 시스템은 차량당 5 msg/s라 **200배 여유**이고, 총량은 차량 수로 늘리므로
   (`vehicle_id`가 태그) 총 처리량과 무관하다. **그래서 고치지 않기로 했다** —
   시퀀스 태그는 포인트마다 시리즈를 만들어 인덱스를 무너뜨리고, `WritePrecision.US`는
   타임스탬프 계약(데이터 스펙 포함)을 바꿔야 한다. 대신 임계값을 `toPoint()` 주석에
   남겨 발행 주기를 줄이려는 사람이 먼저 보게 했다.
   도구·결과: `load-test/storage-integrity/ms_collision.py`,
   `RESULT_20260905_ms_collision.md`.
   **다중 인스턴스 리밸런싱 재전달도 측정 완료(2026-09-05)** —
   `anomaly-detector`를 3 → 1 → 3으로 스케일해 리밸런싱을 일으켰다.
   재전달 **40건(0.100%)**, PostgreSQL 행 증가 **0**, 유실 **0**.
   토픽 초과분·중복 event_id 발행분·`telemetry_anomaly_stored{result="duplicate"}`
   세 출처가 모두 40으로 일치했다. 오늘 고친 "insert 성공에만 브로드캐스트"가
   이 40건의 중복 알림도 걸렀다.
   도구·결과: `load-test/rebalance-redelivery/`.
   남은 갈래: 저장 경로(Java, `telemetry-storage-group`)는 `container_name`·포트
   고정 때문에 스케일이 안 돼 **미측정**, 커밋 배치 크기와 재전달률의 관계 **미측정**,
   실제 OBD-II 동글의 타임스탬프 정밀도 **미확인**(초 단위면 위 분석의 전제가 무너진다)
2. ~~DLQ 재처리 정책, 도구 및 운영 Runbook~~ — **완료(2026-09-04)**.
   DLQ 레코드에 실패 원인 헤더(`x-dlq-*`)를 붙이고(Java·Python 동일 규약),
   `dlq-tools/dlq.py`로 원인별 분류(transient/permanent/unknown)와 재처리를 한다.
   Runbook은 `docs/runbook/dlq-reprocessing.md`.
   **`vehicle-anomaly-alerts-dlq` 중복 알림 검증도 완료(2026-09-05)** —
   PostgreSQL 60초 장애 후 같은 DLQ 레코드를 커서를 바꿔 **두 번** 되돌려도
   행 증가는 0건이었고(`UNIQUE(event_id)` + `ON CONFLICT DO NOTHING`),
   행 수가 토픽의 고유 event_id 수와 정확히 일치했다(16,636 = 16,636, 유실 0).
   측정 도구·결과는 `load-test/anomaly-dlq-idempotency/`.
   **대신 결함을 하나 찾아 고쳤다** — 컨슈머가 `insertIfAbsent` 반환값을 버리고
   무조건 WebSocket 브로드캐스트를 해서, 재처리 때마다 이미 저장된 알림이 다시
   나가고 있었다(1회차 3건, 2회차 9건). `SaveResult(alert, inserted)`로 바꾸고
   지표 `telemetry.anomaly.stored{result=new|duplicate}`를 추가했다.
   현재 Flutter 앱은 이 토픽을 구독하지 않아 실피해는 없었다(잠재 결함).
   같이 알아낸 것: **PostgreSQL 장애는 DLQ를 거의 만들지 않는다** —
   컨슈머가 HikariCP `connectionTimeout`만큼 30초씩 붙잡혀서 60초 장애에 DLQ 9건뿐이고
   나머지는 lag으로 쌓인다. 그리고 그 9건 중 **3건은 이미 저장돼 있었다**
   (서버 커밋은 끝났는데 연결이 끊긴 in-doubt 트랜잭션).
   **300초 장애도 측정 완료(2026-09-05)** — DLQ 0건, 리밸런싱 0건, 유실 0건
   (행 43,347 = 토픽 고유 event_id 43,347). 그 과정에서 **내가 만든 버그**를 찾아
   고쳤다: `AnomalyService`의 클래스 레벨 `@Transactional(readOnly = true)` 때문에
   DB를 안 건드리는 `toEntity()`까지 트랜잭션을 열어, DB 장애 중 **변환 단계에서**
   실패해 레코드가 하나씩 DLQ로 갔다(DLQ 19건 = 고유 알림 8건).
   `NOT_SUPPORTED` + 빈 배치 early return으로 고쳤고 회귀 테스트 2건 추가.
   또 **"180초 예산"이 벽시계가 아님**을 확인했다 — `maxElapsedTime`은 백오프로 쉰
   시간의 합이라, 시도마다 HikariCP 30초를 기다리면 실효 내성이 약 8분이 된다.
   코드·yaml·Runbook의 "총 경과 시간" 서술을 바로잡았다.
   `connectionTimeout` 30초는 **줄이지 않기로 결정** — 원래 근거("줄이면 DLQ가 쌓여
   재처리 절차가 의미를 갖는다")가 사라졌고(300초 장애에 DLQ 0건), 오히려 그 30초가
   백오프 합을 천천히 차게 해 실효 내성 8분을 만들어낸다. 줄이면 DLQ로 더 빨리 간다.
   남은 갈래:
   8분을 넘는 장애에서 리밸런싱이 도는지 **미측정**,
   `vehicle-telemetry-mqtt-dlq`는 payload가 envelope이라 재처리 미지원,
   분류 목록(`TRANSIENT_MARKERS`/`PERMANENT_MARKERS`)은 지금까지 본 예외만 담고 있음
   (PostgreSQL 장애 예외 2종은 이번 실측에서 100% `transient`로 분류돼 문제없었다)
3. ~~MQTT/Kafka/InfluxDB 장애 주입과 복구 후 데이터 정합성 대조~~ — **완료(2026-09-05)**.
   **MQTT 브로커 장애까지 측정했고, 유실 129,445건(72.1%)을 찾아 0으로 고쳤다.**
   정답 기준을 만드는 게 절반이었다 — 시뮬레이터에 `PublishStats`를 넣어
   `publish()` 성공(= paho 큐 적재)과 PUBACK 수신(= 브로커가 받음)을 분리해서 센다.
   원인은 `MqttConfig`에 `setMaxReconnectDelay`가 없어 Paho 기본값 **128초**가 적용된 것.
   `cleanSession=false`라 그동안 브로커가 우리 세션 앞으로 큐잉하다
   `max_queued_messages`(10,000)를 넘기면 말없이 버린다 — 브로커 로그
   `Outgoing messages are being dropped for client telemetry-backend`가 그 순간이다.
   5초로 낮춘 뒤 재측정: 유실 0, 브로커 `$SYS` dropped 0, 복구 후 브로커 수신
   137,702 = backend 수신 증가분 137,702로 정확히 일치.
   결과는 `load-test/fault-injection/RESULT_20260905_mqtt_broker.md`, 회귀 방지는
   `MqttConnectOptionsTest`.
   남은 갈래: 종료 시 flush 45초 안에 못 간 3,542건(발행 측 한계, 파이프라인 유실 아님),
   재연결 실제 소요 시간 분해.
   **`max_queued_messages`도 측정·결정 완료(2026-09-05)** — 90초는 유실 0이었지만
   **300초로 늘리자 17,243건(4.7%)이 다시 버려졌다.** 원인이 다르다: 128초 백오프 때는
   *백엔드가 없어서*였고, 이번엔 2초 만에 붙었는데도 밀린 32만 건이 한꺼번에 쏟아져
   **구독자 드레인 속도를 잠깐 넘어섰기** 때문이다. 같은 증상에 원인이 둘이었고
   첫 번째만 고쳐둔 상태였다. 10,000 → **100,000**으로 올려 유실 0
   (InfluxDB 행 364,802 = 정답 기준 일치, dropped 0). 약 30MB.
   이걸로 유실이 없어지는 게 아니라 뒤로 밀릴 뿐이라, 진짜 방어선은
   `MqttBrokerDroppingMessages` 알림이다(이번에 실제로 값을 냈다).
   **mTLS 프로파일에서도 재측정 완료(2026-09-05)** — 100대 기준 유실 0,
   브로커 `$SYS` dropped 0, 재연결 약 2초로 TLS 핸드셰이크가 붙어도 5초 상한 안이다.
   여기서 **ACL 버그**를 찾았다: 코드는 `$SYS/broker/publish/messages/received`를
   구독하는데 `broker/config/acl`은 옛 토픽만 허용해, **운영 프로파일에서만**
   파이프라인 대조 대시보드 1단계가 비었다(평문은 ACL이 없어 안 드러났다).
   고친 뒤 복구 후 브로커 수신 70,810 = backend 수신 증가분 70,811로 확인.
   측정 도구도 둘 고쳤다 — 복구 판정을 TCP 확인으로(키 파일 0600이라 컨테이너 안에서
   mTLS 클라이언트로 못 붙는다), 시뮬레이터 최종 집계는 종료 마커를 기다린 뒤 읽도록
   (묵은 주기 로그를 읽어 "저장이 발행보다 많다"는 불가능한 결과가 나왔었다).

   (이전 기록 — InfluxDB·Kafka, 2026-09-04)
   InfluxDB·Kafka 90초 장애를 주입해 **둘 다 유실 0**을 확인했다(InfluxDB는 DLQ 재처리로
   84,615 → 161,356 완전 복구, Kafka는 spool이 전량 보관). 도구·결과는
   `load-test/fault-injection/`. **다만 복구 경로가 둘 다 실용적이지 않다** —
   아래 5번 항목 참고(재시도 예산은 그 뒤 180초로 바꿔 해결했다).
4. ~~spool 드레인 속도~~ — **완료(2026-09-04)**. `telemetry.spool.retry-batch`를
   설정으로 빼고 기본값을 100 → **10,000**으로 올렸다(계측 후 결정: 배치 100 = 19 msg/s,
   2,000 = 387 msg/s, 10,000 = 부하 정지 후 약 1분 내 완전 드레인). 스캔은 병목이
   아니었다 — 파일 15만 개에서 회당 121ms로 5초 주기의 2.4%다.
   지표 3종 추가(`telemetry.spool.pending/scan/drained`).
   남은 갈래: 드레인 중에는 새 메시지도 spool로 가므로 **드레인 − 유입**이 실제 감소
   속도인데, 배치 10,000의 이론치 2,000 msg/s는 유입 1,700 대비 여유가 300 msg/s뿐이다.
   더 키우려면 프로듀서 `buffer.memory`(기본 32MB)와 `max.block.ms`를 함께 봐야 하고
   **거기까지는 재지 않았다**
5. ~~재시도 예산 재검토~~ — **완료(2026-09-04)**. `FixedBackOff(1000L, 2L)`(3회/약 2초) →
   `ExponentialBackOff` + **재시도 예산 180초**(`telemetry.kafka.retry.budget-ms`).
   (이 값은 벽시계가 아니라 **백오프로 쉰 시간의 합**이다 — 아래 2번 항목 참고)
   같은 InfluxDB 90초 장애에서 **DLQ 76,878건(47.6%) → 0건**, InfluxDB 행이 토픽 수와
   정확히 일치(180,329)해 수동 재처리가 아예 불필요해졌다. 리밸런싱·백오프 소진 로그 0건.
   예전 값의 근거("한 레코드가 파티션을 막는다")는 이 코드에 해당하지 않았다 —
   레코드 단위 실패는 이미 개별 catch로 DLQ 처리되므로 에러 핸들러까지 오는 건
   의존성 장애뿐이다.
   남은 갈래: 영구 실패 배치가 이제 180초를 붙드는 경우 **미재현**,
   예산을 더 늘리려면 `max.poll.interval.ms`도 올려야 하는데 그러면 죽은 컨슈머 감지가
   느려지는 트레이드오프가 있고 **재지 않았다**
6. ~~발행량·Kafka 유입량·저장 성공량을 한 화면에서 비교하는 관측성 보완~~ —
   **완료(2026-09-04)**. 파이프라인 4단계 대조 대시보드(`monitoring/grafana/dashboards/
   pipeline-funnel.json`) + 알림 3종 추가. 저장 **성공**만 세는
   `telemetry.influx.points.written` 카운터를 새로 넣었다(기존 batch.size.sum은 진입
   시점 기록이라 실패해도 오른다). 12시간 soak 사고를 재현해 `TelemetryStorageStalled`가
   실제로 발동하는 것을 확인했고, 그때 `kafka_consumer_records_lag_max`는 값 자체가 없어
   기존 lag 알림으로는 **원천적으로 못 잡는** 상황이었다. Runbook은
   `docs/runbook/pipeline-observability.md`.
   만들다 기존 버그도 하나 고쳤다 — `MqttIngestFallingBehind`가 `$SYS/broker/messages/received`
   (모든 MQTT 패킷)를 PUBLISH만 센 값과 비교해 **정상 부하에서 계속 울리고 있었다**
   (실측 2배 차이). `publish/messages/received`로 교체.
   남은 갈래: 단계 1은 브로커 전체 수치라 다른 클라이언트가 붙으면 함께 오른다.
   포인트 수와 메시지 수를 직접 비교하는 것은 1:1 구조에서만 성립한다
7. Flutter 앱 — **디자인/반응형 1차 완료(2026-09-05)**, 실기기 검증은 남음.
   앱 저장소(`Seongwonp/vehicle-telemetry-app`)에서 진행했다:
   - 반응형 자동 검사 도입(`test/responsive_overflow_test.dart`) — 구성요소 × 너비 4종 ×
     글자배율 3종. 돌리자마자 overflow 12건이 나왔고 **차량 카드는 320px에서 기본
     글자 크기로도 99px 잘리고 있었다**. 전부 수정
   - 디자인 토큰 도입(`lib/core/theme/design_tokens.dart`, `docs/design-system.md`).
     스케일 벗어난 값 164곳 → 14곳
   - `flutter analyze` 40건 → 0건, 테스트 37 → 181개
   **넓은 화면 가독 폭과 그라데이션 방침도 완료(2026-09-05)**:
   - 화면마다 `maxWidth`를 따로 정해 **8종**(420~1200)이 흩어져 있던 것을
     `ContentWidths` 4단(form 480 / reading 640 / feed 840 / grid 1200)으로 묶었다.
     기준은 폭이 아니라 "한 줄에 무엇이 들어가는가"다. 1280px 스냅샷을 폭 제한
     없는 것과 적용한 것 두 장으로 뽑아 비교했다
   - 폭만 제한하니 계측 카드가 비었다 — 아크 게이지가 104~144px 고정이라 카드가
     커져도 그대로였다. 상한을 220으로 올려 함께 커지게 했다
   - `primaryGradient`는 랜딩·로그인 CTA 두 곳뿐이라 **유지**(진입 화면은 계측
     도구가 아니다). `accentGlow`는 **아무 데서도 안 쓰여서 삭제** — 지우니
     `AppSemanticColors` 전체가 `const`가 됐다
   - 반응형 자동 검사에 데스크톱 폭(1024/1280) 추가. 테스트 181 → 253개, analyze 0
   남은 것: **실기기 검증**(전부 위젯 테스트 기준이다), 스케일 밖 값 14곳 개별 검토,
   대시보드 그리드 열 수(데스크톱 4열)가 새 내용 폭에서 적정한지 미검토

Flutter 앱 고도화는 폐기한 작업이 아니라 후순위 트랙이다. 앱 저장소의 `AGENTS.md`에 적힌
데스크톱 검증 절차를 먼저 수행한 뒤 WebSocket 재연결, 오래된 데이터 표시, 작은 화면
overflow, 접근성, 오류·빈 상태를 우선 개선한다. 새 화면이나 보여주기용 기능 추가는 그 뒤다.

---

## 차량 데이터 스펙

```json
{
  "vehicle_id": "KR-GA-1234",
  "timestamp": "2026-05-09T10:00:00Z",
  "speed": 87.3,
  "rpm": 2400,
  "engine_temp": 92.1,
  "throttle_position": 34.5,
  "fuel_level": 67.0,
  "battery_voltage": 13.8,
  "gps": {
    "lat": 37.123456,
    "lng": 127.654321
  },
  "dtc_codes": []
}
```

---

## 이상 감지 룰 (Phase 3 기준)

| 항목 | 이상 조건 |
|------|----------|
| 엔진 온도 | 105°C 초과 |
| RPM | 6000 초과 |
| 배터리 전압 | 11.5V 미만 또는 15V 초과 |
| 속도 | 200km/h 초과 |
| DTC 코드 | 배열이 비어있지 않을 때 |

---

## AI에게 요청할 때 참고사항

- 언어 기본값: **Java (Spring Boot)**, **Python**, **C** (시뮬레이터)
- 커밋과 푸시는 기본적으로 명령어만 안내하고, 사용자가 명시적으로 요청한 경우에만 직접 실행할 것
- 코드 생성 시 보안 취약점 (SQL Injection, IDOR, 인증 누락) 반드시 검토
- 새 기능 추가 전 현재 Phase가 완료됐는지 확인
- 테스트 코드는 JUnit 5 (Java), pytest (Python) 사용
- 환경변수는 `.env` 파일로 분리, 하드코딩 금지
- Docker Compose로 전체 스택 실행 가능하게 유지

---

## 구현 단계 (Phase)

### Phase 1 — 데이터 수집 파이프라인
- 차량 시뮬레이터 (MQTT Publisher)
- Mosquitto 브로커 구축 (TLS 적용)
- Kafka 클러스터 구성
- Spring Boot Kafka Consumer → InfluxDB 저장

### Phase 2 — REST API 서버
- 차량 등록/관리 API
- 실시간 데이터 조회 API
- JWT 인증/인가
- Rate Limiting

### Phase 3 — 이상 감지
- 룰 기반 이상 감지 + Kafka 이벤트 발행
- 알림 (Webhook 또는 이메일)
- (선택) Isolation Forest 머신러닝 감지

### Phase 4 — 보안 강화
- MQTT X.509 인증서 인증
- API 요청 로깅 + 이상 접근 감지
- 보안 취약점 자체 점검 및 보고서

### Phase 5 — 모니터링 & 배포
- Grafana 대시보드
- Prometheus + Spring Actuator
- Docker Compose 전체 컨테이너화
- AWS EC2 배포 (선택)
